own-app-api:
  replicaCount: 1
  image:
    repository: "striveonger/own-app-api"
    tag: "1.0.0"
  probe:
    enabled: true
  config:
    applicationYaml:
      own:
        app:
          storage:
            memory:
              max-rows: 10
  env:
    - name: SPRING_PROFILES_ACTIVE
      value: "dev"
    - name: SF_API_KEY
      value: "sk-xyz"

own-app-ui:
  replicaCount: 1
  image:
    repository: "striveonger/own-app-ui"
    tag: "1.0.0"
