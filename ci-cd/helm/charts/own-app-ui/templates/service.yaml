apiVersion: v1
kind: Service
metadata:
  name: {{ include "own-app-ui.fullname" . }}
  labels:
    {{- include "own-app-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "own-app-ui.selectorLabels" . | nindent 4 }}
